name: Build for App Stores

on:
  push:
    branches: ['release']

jobs:
  # Build Android production
  build_android:
    type: build
    params:
      platform: android
      profile: production

  # Build iOS production
  build_ios:
    type: build
    params:
      platform: ios
      profile: production
